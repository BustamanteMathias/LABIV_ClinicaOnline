import { FilterProfesionalPipe } from './filter-profesional.pipe';

describe('FilterProfesionalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfesionalPipe();
    expect(pipe).toBeTruthy();
  });
});
